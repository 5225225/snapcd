[workspace]

members = [
    "snapcd",
    "snapcdd",
    "libsnapcd",
]

default-members=["snapcd"]

[profile.release]
debug = true

# Speed our tests up by a bit
[profile.test.package."*"]
opt-level = 3
